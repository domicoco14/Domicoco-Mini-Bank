# 🏦 Domicoco Mini Banking System

A lightweight, terminal-based banking application built with Python. This project manages basic financial operations through a clean and interactive command-line interface.

## ✨ Features
* **Secure Account Management:** Core banking logic handled via `edge_bank.py`.
* **Dynamic ASCII Interface:** Beautiful headers and styling using the `art` library.
* **User Operations:** Support for account initialization, deposits, and withdrawals.
* **Terminal Driven:** Fast execution directly from your console without needing a browser.

## 🚀 Getting Started

### Prerequisites
Ensure you have Python installed on your system. You will also need to install the `art` library for the visual headers:
```bash
pip install art
